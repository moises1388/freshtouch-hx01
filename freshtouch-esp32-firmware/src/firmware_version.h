#pragma once
// Versión de firmware — se sube a mano en cada release, se reporta en
// /status. No confundir con la versión del protocolo HTTP (que no ha
// cambiado desde HX01: ver docs/PROTOCOL.md).
#define FRESHTOUCH_FIRMWARE_VERSION "3.0.0-hx02-discovery"
